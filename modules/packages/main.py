from utils.helpers import sum, diff
from utils import helpers
from utils.logs.console import log

print(sum(1, 2))
print(diff(1, 2))

print(helpers.sum(1, 2))
print(helpers.diff(1, 2))
print(helpers.product(1, 2))

log("Hello World")
